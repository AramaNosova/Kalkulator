<Window x:Class="SportsRentalSystem.WindowAddClient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SportsRentalSystem"
        mc:Ignorable="d"
        Title="Добавление клиента" 
        Height="450" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Используем те же стили, что и для сотрудников -->
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="10" 
            BorderThickness="1" 
            BorderBrush="#E0E0E0"
            Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="3" Color="#40000000"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" 
                    Background="#2196F3" 
                    CornerRadius="8,8,0,0" 
                    Padding="20,15"
                    Margin="0,0,0,20">
                <DockPanel>
                    <TextBlock Text="👤 Добавить клиента" 
                              FontSize="18" 
                              FontWeight="Bold" 
                              Foreground="White"/>
                    <Button x:Name="btnClose" 
                           Content="✕" 
                           Background="Transparent" 
                           BorderThickness="0"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="Bold"
                           Width="30"
                           Height="30"
                           DockPanel.Dock="Right"
                           Cursor="Hand"
                           Click="btnClose_Click"/>
                </DockPanel>
            </Border>

            <!-- Форма -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Личная информация -->
                    <GroupBox Header="👤 Личная информация" FontWeight="SemiBold" Margin="0,0,0,15">
                        <StackPanel Margin="10">
                            <TextBlock Text="Фамилия*" Margin="0,0,0,5" Foreground="#666"/>
                            <TextBox x:Name="txtLastName" Style="{StaticResource ModernTextBox}"/>

                            <TextBlock Text="Имя*" Margin="0,15,0,5" Foreground="#666"/>
                            <TextBox x:Name="txtFirstName" Style="{StaticResource ModernTextBox}"/>

                            <TextBlock Text="Отчество" Margin="0,15,0,5" Foreground="#666"/>
                            <TextBox x:Name="txtMiddleName" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Контактная информация -->
                    <GroupBox Header="📞 Контактная информация" FontWeight="SemiBold">
                        <StackPanel Margin="10">
                            <TextBlock Text="Телефон*" Margin="0,0,0,5" Foreground="#666"/>
                            <TextBox x:Name="txtPhone" Style="{StaticResource ModernTextBox}"/>

                            <TextBlock Text="Email" Margin="0,15,0,5" Foreground="#666"/>
                            <TextBox x:Name="txtEmail" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>

            <!-- Кнопки -->
            <Border Grid.Row="2" 
                    Background="#F9F9F9" 
                    CornerRadius="0,0,8,8" 
                    Padding="20,15"
                    Margin="0,20,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnCancel" 
                           Content="Отмена" 
                           Style="{StaticResource CancelButton}"
                           Margin="0,0,10,0"
                           Click="btnCancel_Click"/>
                    <Button x:Name="btnSave" 
                           Content="Сохранить клиента" 
                           Style="{StaticResource ActionButton}"
                           Click="btnSave_Click" Height="41" Margin="0,0,0,0.333" VerticalAlignment="Bottom"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>